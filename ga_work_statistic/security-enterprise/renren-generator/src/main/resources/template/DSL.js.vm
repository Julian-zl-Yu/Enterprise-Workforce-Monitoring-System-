import {
    required
} from "@/utils/formRule";

export const MODULE_BASE_URL = "/${moduleName}/${pathName}";

/* 列表 表头 */
export let DSLTableHeader = [
    #foreach($column in $columns)
        {prop: "${column.attrname}", label: "${column.comments}"}
        #if($velocityCount != $columns.size()),#end
    #end

];

/* 查询框 */
export let DSLFormItemsQuery = {
    ${pk.attrname}:{
        prop: "${pk.attrname}",
        span: 8,
        label: "${pk.comments}"
}
};

/* 详情表单 */
export let DSLFormItems = {

#foreach($column in $columns)
    ${column.attrname}: {
      prop: "${column.attrname}",
      label: "${column.comments}"
    }#if($velocityCount != $columns.size()),
#end
#end

};


/* 详情表单 验证规则 */

export let DSLFormItemRule = {
#foreach($column in $columns)
    ${column.attrname}:[required]
    #if($velocityCount != $columns.size()),
    #end
#end
};